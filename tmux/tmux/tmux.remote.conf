# show status bar at bottom for remote session, 
# so it do not stack together with local session's one
set -g status-position bottom

if-shell "test -f ~/.tmux/tmuxline.remote" "source ~/.tmux/tmuxline.remote"
